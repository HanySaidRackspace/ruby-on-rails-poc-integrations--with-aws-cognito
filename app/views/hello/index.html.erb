<% email = @user.present? ? @user.email : 'Stranger' %>
<% id = @user.present? ? @user.id : 'Stranger' %>
<p>
  <h1> <%= @greeting %> </h1>
<h1> <%=@clientId %> </h1>

<h3> current user email :   <%= email %></h3>

<h3> current user ID  :   <%= id %></h3>
</p>